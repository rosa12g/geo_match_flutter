name: geo_match_flutter
description: A production-ready realtime location matching engine for Flutter. Finds and assigns the nearest available user — ideal for ride-hailing, delivery, or emergency apps.
version: 1.0.1
homepage: https://github.com/rosa12g/geo_match_flutter
repository: https://github.com/rosa12g/geo_match_flutter

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
